spring:
  application:
    name: "Voz Cidadã"
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/voz-cidada?useSSL=false&serverTimezone=UTC
    username: root
    password: sacola07
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: false